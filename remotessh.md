## VS Code で Remote SSH 接続する方法

この手順では、Visual Studio Code（VS Code）を使って **hokushin サーバに SSH 接続**します。

---

## 1. VS Code に拡張機能をインストール

### 1.1 拡張機能画面を開く

VS Code を起動し、左側のメニューから **拡張機能（Extensions）** を開きます。

### 1.2 Remote - SSH をインストール

検索欄に以下を入力します。

```
Remote - SSH
```

提供元が **Microsoft** のものをインストールしてください。

---

## 2. Remote SSH でサーバに接続

### 2.1 コマンドパレットを開く

以下のいずれかで **コマンドパレット**を開きます。

* macOS：`Cmd + Shift + P`
* Windows / Linux：`Ctrl + Shift + P`

---

### 2.2 SSH 接続コマンドを実行

コマンドパレットに次を入力します。

```
Remote-SSH: Connect to Host...
```

表示された一覧から、次を選択します。

```
hokushin
```

※ この `hokushin` は、**~/.ssh/config に書いた Host 名**です。

---

### 2.3 接続先 OS の選択（初回のみ）

初回接続時に、接続先の OS を聞かれることがあります。

```
Linux
```

を選択してください。

---

### 2.4 接続完了

接続が成功すると：

* 新しい VS Code ウィンドウが開く
* 左下に `SSH: hokushin` と表示される

これで **リモートサーバ上での作業**が可能になります。

---

## 3. リモート上のフォルダを開く

接続後、次の操作を行います。

1. メニューから
   **File → Open Folder**
2. サーバ上の作業ディレクトリ（例：`/home/自分のユーザー名`）を選択
3. 「OK」を押す

※ **最初は必ず自分のホームディレクトリを選んでください**

---

## 4. 作業中の注意点

* VS Code 上で編集・保存したファイルは
  **すべてサーバ上のファイル**です
* ローカル PC とは別物なので注意してください
* ターミナル（``Ctrl + ` ``）もサーバ上で実行されます

---

## 5. VS Code から安全に接続を切る方法（重要）

作業終了時は、**必ず以下の方法で切断**してください。

---

### 方法①：VS Code の操作で切断（推奨）

1. 左下の
   `SSH: hokushin`
   をクリック
2. 表示されたメニューから
   **Close Remote Connection**
   を選択

→ 自動的にローカル環境へ戻ります。

---

### 方法②：コマンドパレットから切断

1. コマンドパレットを開く

   * macOS：`Cmd + Shift + P`
   * Windows / Linux：`Ctrl + Shift + P`
2. 以下を入力

```
Remote-SSH: Close Remote Connection
```

---

### ⚠ やってはいけないこと

* VS Code を**強制終了**するだけ
* ノート PC を**スリープにするだけ**

これらは **SSH 接続が残る**場合があり、サーバに負荷がかかります。

---

## 6. 接続が切れているかの確認

切断後：

* 左下の表示が
  `SSH: hokushin` → 何も表示されない
  または
  `><` のようなローカル表示に戻る

これで **正常に切断完了**です。

---

## トラブル時のチェックポイント

* `~/.ssh/config` の設定が正しいか
* ユーザー名・秘密鍵のパスが **自分用**になっているか
* ターミナルで `ssh hokushin` が通るか

---
